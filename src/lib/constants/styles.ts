import { SxProps } from '@mui/system';

export const defaultBgColor = 'background.default';

export const ellipsisOverflow: SxProps = {
    overflow: 'hidden',
    whiteSpace: 'nowrap',
    textOverflow: 'ellipsis',
};
